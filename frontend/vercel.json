{
  "framework": "nextjs",
  "installCommand": "npm install && npm run db:generate --prefix ../backend",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/cron/weekly-training-reminders",
      "schedule": "0 9 * * 1"
    }
  ]
}
